import{Text,StyleSheet,TextInput,ScrollView}from'react-native';import{router}from'expo-router';import{useState}from'react';import{useMutation,useQueryClient}from'@tanstack/react-query';import{ScreenContainer}from'../../../src/components/common/ScreenContainer';import{PrimaryButton}from'../../../src/components/common/PrimaryButton';import{friendlyApi,friendlyKeys}from'../../../src/features/player/friendly';import{validateFriendly}from'../../../src/features/player/friendlyValidation';import{colors}from'../../../src/theme';export default function Create(){const[v,setV]=useState({title:'',description:'',eventType:'SINGLES',format:'LEAGUE',maxPlayers:'6'});const[error,setError]=useState('');const c=useQueryClient();const m=useMutation({mutationFn:()=>friendlyApi.create({...v,maxPlayers:Number(v.maxPlayers)}),onSuccess:(x:any)=>{void c.invalidateQueries({queryKey:friendlyKeys.all});router.replace({pathname:'/(player)/friendly/[id]',params:{id:String(x.id)}})},onError:(e:any)=>setError(e?.response?.data?.message||'Unable to create match.')});return <ScreenContainer><ScrollView><Text style={s.title}>Create Friendly Match</Text><TextInput placeholder="Title"value={v.title}onChangeText={x=>setV({...v,title:x})}style={s.input}/><TextInput placeholder="Description"value={v.description}onChangeText={x=>setV({...v,description:x})}style={s.input}/><TextInput placeholder="Max players"value={v.maxPlayers}onChangeText={x=>setV({...v,maxPlayers:x})}style={s.input}keyboardType="number-pad"/><Text onPress={()=>setV({...v,eventType:v.eventType==='SINGLES'?'DOUBLES':'SINGLES'})}style={s.choice}>Event: {v.eventType}</Text><Text onPress={()=>setV({...v,format:v.format==='LEAGUE'?'KNOCKOUT':'LEAGUE'})}style={s.choice}>Format: {v.format}</Text>{error&&<Text style={s.error}>{error}</Text>}<PrimaryButton disabled={m.isPending}title={m.isPending?'Creating…':'Create'}onPress={()=>{if(m.isPending)return;const e=validateFriendly({title:v.title,eventType:v.eventType,maxPlayers:Number(v.maxPlayers)});if(e)return setError(e);m.mutate()}}/></ScrollView></ScreenContainer>}const s=StyleSheet.create({title:{fontSize:30,fontWeight:'800',color:colors.text,marginTop:35},input:{backgroundColor:colors.white,borderRadius:12,padding:14,marginTop:12},choice:{backgroundColor:colors.white,padding:15,borderRadius:12,marginTop:12,color:colors.text},error:{color:colors.error,marginVertical:12}});
