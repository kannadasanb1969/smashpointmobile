import { Text, View, StyleSheet } from 'react-native'; import { router } from 'expo-router'; import { ScreenContainer } from '../src/components/common/ScreenContainer'; import { PrimaryButton } from '../src/components/common/PrimaryButton'; import { colors, spacing } from '../src/theme';
export default function DevEntry(){return <ScreenContainer><Text style={s.badge}>DEVELOPMENT ONLY</Text><Text style={s.title}>Choose a role area</Text><Text style={s.copy}>Temporary visual navigation. This is not authentication.</Text><View style={s.stack}><PrimaryButton title="Player home" onPress={()=>router.push('/(player)')}/><PrimaryButton title="Organizer home" onPress={()=>router.push('/(organizer)')}/><PrimaryButton title="Admin home" onPress={()=>router.push('/(admin)')}/></View></ScreenContainer>} const s=StyleSheet.create({badge:{color:colors.warning,fontWeight:'800',marginTop:80,letterSpacing:1},title:{fontSize:30,fontWeight:'800',color:colors.text,marginTop:spacing.sm},copy:{color:colors.muted,fontSize:16,marginTop:spacing.sm},stack:{gap:spacing.sm,marginTop:spacing.lg}});
