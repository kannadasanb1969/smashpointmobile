import { ActivityIndicator, Text, View } from 'react-native'; import { PrimaryButton } from '../common/PrimaryButton'; import { colors } from '../../theme'; export function QueryState({loading,error,empty,onRetry}:{loading:boolean;error:boolean;empty?:boolean;onRetry:()=>void}){if(loading)return <ActivityIndicator accessibilityLabel="Loading" size="large" color={colors.primary}/>;if(error)return <View><Text style={{color:colors.error,marginBottom:12}}>Unable to load data.</Text><PrimaryButton title="Try again" onPress={onRetry}/></View>;if(empty)return <Text style={{color:colors.muted,textAlign:'center',padding:30}}>Nothing here yet.</Text>;return null;}
