import{apiClient}from'../../api/apiClient';export const canCompleteMatch=(a:number,b:number,target:number)=>Math.max(a,b)>=target&&Math.abs(a-b)>=2;export const scoringActions=(x:{authorized:boolean;status:string;scoreA:number;scoreB:number;target:number})=>({readOnly:!x.authorized||x.status==='COMPLETED',canStart:x.authorized&&x.status==='SCHEDULED',canIncrement:x.authorized&&x.status==='LIVE',canDecrement:(side:'A'|'B')=>x.authorized&&x.status==='LIVE'&&(side==='A'?x.scoreA:x.scoreB)>0,canComplete:x.authorized&&x.status==='LIVE'&&canCompleteMatch(x.scoreA,x.scoreB,x.target)});export const scoreAllowed=(score:number,action:'INCREMENT'|'DECREMENT')=>action==='INCREMENT'||score>0;export const scoringApi={match:async(id:string)=>(await apiClient.get(`/api/matches/${id}`)).data,start:async(id:string,payload:unknown={})=>(await apiClient.post(`/api/matches/${id}/start`,payload)).data,score:async(id:string,payload:{side:'A'|'B';action:'INCREMENT'|'DECREMENT'})=>(await apiClient.post(`/api/matches/${id}/score`,payload)).data,complete:async(id:string,payload:unknown={})=>(await apiClient.post(`/api/matches/${id}/complete`,payload)).data};
export const isStaleScoringError=(error:any,operation:'start'|'score'|'complete')=>error?.status===409&&((operation==='start'&&/already started|must be scheduled/i.test(String(error.message)))||(operation==='score'&&/must be live|already completed/i.test(String(error.message)))||(operation==='complete'&&/already completed|must be live/i.test(String(error.message))));
