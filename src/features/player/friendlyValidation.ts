export const validateFriendly=(v:{title:string;eventType:string;maxPlayers:number})=>{if(!v.title.trim())return'Title is required.';if(!Number.isInteger(v.maxPlayers))return'Max players must be a whole number.';if(v.eventType==='SINGLES'&&(v.maxPlayers<6||v.maxPlayers>16))return'Singles requires 6 to 16 players.';if(v.eventType==='DOUBLES'&&(v.maxPlayers<8||v.maxPlayers>16))return'Doubles requires 8 to 16 players.';if(v.eventType==='DOUBLES'&&v.maxPlayers%2)return'Doubles requires an even number of players.';return null};
