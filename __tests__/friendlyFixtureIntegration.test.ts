import{resolveFriendlySide,createInFlightGuard,getFriendlyDetailsActions}from'../src/features/player/friendly';test('fixture screen production integration helpers resolve sides and guard rapid generation',()=>{expect(resolveFriendlySide('p1',[{id:'p1',full_name:'A'}],[])).toBe('A');const g=createInFlightGuard();expect(g.tryStart()).toBe(true);expect(g.tryStart()).toBe(false);g.release();expect(g.tryStart()).toBe(true);expect(getFriendlyDetailsActions({isCreator:true,eventType:'DOUBLES',isReady:true,hasFixtures:false}).showManagePairings).toBe(true)});
